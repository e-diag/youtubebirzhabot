# 🔒 Инструкция по очистке сервера после попытки взлома

## ✅ Что уже сделано:

1. **PostgreSQL закрыт от публичного доступа** - порт 5432 доступен только внутри Docker сети
2. **Redis закрыт от публичного доступа** - порт 6379 доступен только внутри Docker сети
3. **Добавлен мониторинг безопасности** - автоматическое отслеживание подозрительной активности
4. **Усилена безопасность PostgreSQL** - отключено выполнение программ

## 📋 Пошаговая инструкция по очистке сервера:

### Шаг 1: Проверка на компрометацию

Выполните на сервере следующие команды для проверки:

```bash
# 1. Проверьте запущенные процессы на подозрительную активность
ps aux | grep -E "bot|curl|wget|195.24.237.73|base64|bash.*195"

# 2. Проверьте сетевые соединения с подозрительным IP
netstat -tulpn | grep -E "195.24.237.73|195\.24\.237\.73"

# 3. Проверьте файлы в /tmp на наличие backdoor
ls -la /tmp/bot* /tmp/*.sh 2>/dev/null
find /tmp -name "*bot*" -o -name "*.sh" 2>/dev/null

# 4. Проверьте логи PostgreSQL на другие атаки
docker compose logs postgres | grep -iE "COPY|PROGRAM|EXECUTE|FROM PROGRAM|base64|bash"

# 5. Проверьте, открыт ли порт 5432 публично (должен быть закрыт)
sudo netstat -tulpn | grep 5432
# Если видите 0.0.0.0:5432 - это плохо! Должно быть только 127.0.0.1:5432 или вообще не видно

# 6. Проверьте firewall
sudo ufw status
# или
sudo iptables -L -n | grep 5432
```

### Шаг 2: Если обнаружены признаки компрометации

**⚠️ ВНИМАНИЕ: Если найдены подозрительные процессы, файлы или соединения:**

1. **Немедленно отключите сервер от интернета** (если возможно)
2. **Создайте резервную копию данных БД:**
   ```bash
   docker compose exec postgres pg_dump -U postgres youtube_market > backup_$(date +%Y%m%d_%H%M%S).sql
   ```
3. **Скопируйте резервную копию на безопасное место**
4. **Переустановите сервер с нуля**
5. **Восстановите данные из резервной копии**

### Шаг 3: Если сервер чист (нет признаков компрометации)

#### 3.1. Смените все пароли

```bash
# На сервере отредактируйте .env файл
nano .env

# Смените следующие пароли на новые сильные (минимум 32 символа):
POSTGRES_PASSWORD=новый_сильный_пароль_минимум_32_символа
POSTGRES_USER=postgres  # Можно оставить или изменить
GRAFANA_ADMIN_PASSWORD=новый_сильный_пароль

# Сохраните файл (Ctrl+O, Enter, Ctrl+X)
```

#### 3.2. Обновите код на сервере

```bash
# На сервере перейдите в директорию проекта
cd /root/youtubebirzha  # или ваш путь

# Обновите код из GitHub
git pull origin main

# Убедитесь, что порты закрыты в docker-compose.yml
# Проверьте, что строки с ports: закомментированы для postgres и redis
```

#### 3.3. Перезапустите контейнеры

```bash
# Остановите контейнеры
docker compose down

# Убедитесь, что старые контейнеры удалены
docker ps -a | grep postgres
docker ps -a | grep redis

# Запустите с новой конфигурацией
docker compose up -d --build

# Проверьте логи
docker compose logs -f
```

#### 3.4. Проверьте, что порты закрыты

```bash
# Проверьте, что порт 5432 НЕ открыт публично
sudo netstat -tulpn | grep 5432
# Должно быть пусто или только 127.0.0.1:5432

# Проверьте, что порт 6379 НЕ открыт публично
sudo netstat -tulpn | grep 6379
# Должно быть пусто или только 127.0.0.1:6379

# Проверьте firewall
sudo ufw status
# Убедитесь, что порты 5432 и 6379 НЕ открыты
```

#### 3.5. Настройте firewall (если не настроен)

```bash
# Установите ufw (если не установлен)
sudo apt-get update
sudo apt-get install -y ufw

# Разрешите только необходимые порты
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP (если используете)
sudo ufw allow 443/tcp   # HTTPS (если используете)
sudo ufw allow 8080/tcp  # Ваше приложение (если нужно)

# НЕ открывайте порты 5432 и 6379!

# Включите firewall
sudo ufw enable

# Проверьте статус
sudo ufw status
```

### Шаг 4: Проверка безопасности после очистки

```bash
# 1. Проверьте, что приложение работает
curl http://localhost:8080/health

# 2. Проверьте логи на ошибки
docker compose logs app | tail -50

# 3. Проверьте, что мониторинг безопасности работает
docker compose logs app | grep -i "security\|monitoring"

# 4. Проверьте подключение к БД из приложения
docker compose logs app | grep -i "database.*success\|connected to database"
```

### Шаг 5: Настройка мониторинга

После деплоя бот будет автоматически:
- ✅ Отслеживать подозрительную активность в логах PostgreSQL
- ✅ Отправлять уведомления о критических событиях безопасности
- ✅ Проверять логи каждые 30 секунд
- ✅ Проверять прошлые атаки при старте

### Шаг 6: Регулярная проверка

Рекомендуется регулярно (раз в неделю):

```bash
# Проверяйте логи на подозрительную активность
docker compose logs postgres | grep -iE "COPY|PROGRAM|EXECUTE|FROM PROGRAM"

# Проверяйте процессы
ps aux | grep -E "bot|curl|wget|base64"

# Проверяйте сетевые соединения
netstat -tulpn | grep -E "195.24.237.73"
```

## 🔒 Дополнительные меры безопасности:

### 1. Используйте сильные пароли

- Минимум 32 символа
- Случайные символы, цифры, буквы
- Не используйте одинаковые пароли для разных сервисов

### 2. Регулярно обновляйте

```bash
# Обновляйте Docker образы
docker compose pull

# Обновляйте систему
sudo apt-get update && sudo apt-get upgrade -y
```

### 3. Настройте автоматические резервные копии

```bash
# Создайте скрипт для резервного копирования
cat > /root/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
docker compose exec -T postgres pg_dump -U postgres youtube_market > /root/backups/backup_$DATE.sql
# Удалите старые резервные копии (старше 7 дней)
find /root/backups -name "backup_*.sql" -mtime +7 -delete
EOF

chmod +x /root/backup.sh

# Добавьте в cron (каждый день в 3:00)
(crontab -l 2>/dev/null; echo "0 3 * * * /root/backup.sh") | crontab -
```

### 4. Мониторинг через Telegram

Убедитесь, что в `.env` настроены:
```bash
BOT_TOKEN=your_bot_token
NOTIFY_CHAT_ID=your_chat_id
```

После этого бот будет отправлять уведомления о:
- 🚨 Критических ошибках
- ⚠️ Предупреждениях
- 🔴 Событиях безопасности
- ℹ️ Информационных сообщениях

## ✅ Чек-лист безопасности:

- [ ] Порт 5432 (PostgreSQL) закрыт от публичного доступа
- [ ] Порт 6379 (Redis) закрыт от публичного доступа
- [ ] Все пароли изменены на сильные
- [ ] Firewall настроен и включен
- [ ] Код обновлен до последней версии
- [ ] Контейнеры перезапущены
- [ ] Мониторинг безопасности работает
- [ ] Резервные копии настроены
- [ ] Telegram уведомления настроены

## 🆘 Если что-то пошло не так:

1. Проверьте логи: `docker compose logs`
2. Проверьте статус контейнеров: `docker compose ps`
3. Проверьте подключение к БД: `docker compose exec postgres psql -U postgres -d youtube_market -c "SELECT 1;"`
4. Проверьте настройки в `.env` файле

---

**ВАЖНО:** После выполнения всех шагов сервер будет защищен от подобных атак. PostgreSQL и Redis будут доступны только изнутри Docker сети, что значительно повышает безопасность.

