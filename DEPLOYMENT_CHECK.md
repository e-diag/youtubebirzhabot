# 🔍 Проверка деплоя через GitHub Actions

## Что должно происходить:

При `git push origin main` автоматически запускается:

1. ✅ **GitHub Actions workflow** (`.github/workflows/ci-cd.yml`)
2. ✅ **Сборка Docker образа**
3. ✅ **Публикация в GitHub Packages**
4. ✅ **Автоматический деплой на сервер**

## Почему может не работать:

### 1. Нет новых коммитов

Если вы видите "Everything up-to-date", значит:
- Все коммиты уже были отправлены ранее
- Нет новых изменений для отправки

**Решение:**
```bash
# Проверьте, есть ли незакоммиченные изменения
git status

# Если есть изменения, закоммитьте их:
git add .
git commit -m "Описание изменений"
git push origin main
```

### 2. GitHub Actions не запустился

**Проверьте:**
1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Actions** (вкладка вверху)
3. Должен быть запущен workflow **"CI/CD Pipeline"**

**Если workflow не запустился:**
- Убедитесь, что файл `.github/workflows/ci-cd.yml` есть в репозитории
- Проверьте, что вы пушите в ветку `main` или `master`

### 3. Не настроены Secrets

Для деплоя нужны secrets в GitHub:

**Проверьте:**
1. GitHub → Settings → Secrets and variables → Actions
2. Должны быть:
   - `SSH_PRIVATE_KEY` - приватный SSH ключ
   - `SERVER_HOST` - IP или домен сервера
   - `SERVER_USER` - пользователь SSH (обычно `root`)
   - `SERVER_PATH` - путь к проекту (например `/root/youtube-market`)
   - `SERVER_URL` - URL для health check (опционально)

**Если secrets не настроены:**
- Workflow запустится, но деплой упадет с ошибкой
- Проверьте логи в GitHub Actions

### 4. Workflow запустился, но деплой упал

**Проверьте логи:**
1. GitHub → Actions
2. Откройте последний запуск workflow
3. Нажмите на job **"Deploy to Production"**
4. Проверьте ошибки в логах

**Частые ошибки:**
- ❌ SSH подключение не работает
- ❌ Неправильный `SERVER_PATH`
- ❌ Docker не установлен на сервере
- ❌ Нет прав на выполнение команд

## Как проверить, что деплой работает:

### Шаг 1: Проверьте GitHub Actions

```bash
# Откройте в браузере:
# https://github.com/YOUR_USERNAME/YOUR_REPO/actions
```

Должен быть запущен workflow после последнего push.

### Шаг 2: Создайте тестовый коммит

```bash
# Создайте небольшое изменение
echo "# Test" >> test.txt

# Закоммитьте и отправьте
git add test.txt
git commit -m "Test deployment"
git push origin main
```

### Шаг 3: Проверьте логи

1. GitHub → Actions → последний workflow
2. Проверьте, что:
   - ✅ Job "Build and Push Docker Image" успешен (зеленая галочка)
   - ✅ Job "Deploy to Production" успешен (зеленая галочка)

### Шаг 4: Проверьте сервер

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Проверьте, что образ обновился
docker images | grep ghcr.io

# Проверьте логи приложения
docker compose logs app --tail 50
```

## Ручной запуск деплоя:

Если автоматический деплой не работает, можно запустить вручную:

1. GitHub → Actions
2. Выберите **"CI/CD Pipeline"**
3. Нажмите **"Run workflow"**
4. Выберите ветку `main`
5. Нажмите **"Run workflow"**

## Troubleshooting:

### Workflow не виден в Actions

**Причина:** Файл `.github/workflows/ci-cd.yml` не в репозитории

**Решение:**
```bash
# Проверьте, что файл есть
ls -la .github/workflows/

# Если нет, добавьте и закоммитьте:
git add .github/workflows/ci-cd.yml
git commit -m "Add CI/CD workflow"
git push origin main
```

### "Permission denied" при SSH

**Причина:** Неправильный SSH ключ или нет доступа

**Решение:**
1. Проверьте, что `SSH_PRIVATE_KEY` правильный
2. Убедитесь, что публичный ключ добавлен в `authorized_keys` на сервере
3. Проверьте права: `chmod 600 ~/.ssh/authorized_keys`

### "Image not found" при деплое

**Причина:** Образ не был собран или не опубликован

**Решение:**
1. Проверьте, что job "Build and Push" успешен
2. Проверьте GitHub Packages: `https://github.com/YOUR_USERNAME/YOUR_REPO/pkgs/container`
3. Убедитесь, что используется правильное имя образа

### Health check не проходит

**Причина:** Приложение не запустилось или недоступно

**Решение:**
```bash
# На сервере проверьте:
docker compose ps
docker compose logs app

# Проверьте health endpoint:
curl http://localhost:8080/health
```

## Быстрая проверка:

```bash
# 1. Проверьте статус
git status

# 2. Если есть изменения, закоммитьте
git add .
git commit -m "Your changes"
git push origin main

# 3. Проверьте GitHub Actions через 1-2 минуты
# https://github.com/YOUR_USERNAME/YOUR_REPO/actions
```

